Place these files here (used as the mini game-card thumbnails inside the
hero mockup in `src/components/HeroVisual.tsx`):

- `tycoon.webp`
- `anime.webp`
- `pet-world.webp`
- `city-life.webp`

These are decorative thumbnails only — no price/revenue numbers are shown
next to them in the hero mockup. They are not wired into the real
"Featured Roblox Games" section on the homepage (`FeaturedGamesSection`),
which intentionally shows an honest empty state until real listings exist.
Ask if you'd like these used there instead — that would mean showing
specific prices/revenue as example data rather than real listings.
